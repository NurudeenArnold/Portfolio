{
  "name": "portfolio-backend",
  "private": true,
  "scripts": {
    "build": "echo 'no build step needed, api/ functions are deployed as-is'",
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "@vercel/node": "^13.0.1",
    "typescript": "^5.5.4"
  }
}
